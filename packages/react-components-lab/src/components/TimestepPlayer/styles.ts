import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  annotations: {
    fontSize: 12,
    fontWeight: 600,
  },
  icon: {
    height: 38,
    width: 38,
  },
}));
