import { styled } from '@mui/material/styles';
import { Card } from '@mui/material';

export default styled(Card)({
  borderRadius: 4,
  marginTop: 12,
  marginBottom: 15,
  position: 'relative',
});
