import { FC } from 'react';
import { ColorsArr, LegendProps } from './types';
export declare const mixLegendColors: (colorsArr: ColorsArr) => ColorsArr;
declare const Legend: FC<LegendProps>;
export default Legend;
